<?php

/**
 * English is the source language: the keys are the English strings as they
 * appear in the code, and an untranslated key renders as itself.
 *
 * Empty on purpose. The control panel titles the Statamic addon shows in title
 * case are not applied here: Craft CMS titles its own fields that way but keeps
 * actions in sentence case, and half a convention reads worse than none.
 */
return [];
